{
  "name": "tailwind-demo",
  "version": "1.0",
  "devDependencies": {
    "tailwindcss": "^3.2.1"
  },

  "scripts": {
    "create-config": "npx tailwindcss init",
    "build-css": "npx tailwindcss-cli@latest build -i ./src/style.css -o ./dist/style.css"
  }
}
